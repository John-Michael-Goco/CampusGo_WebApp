<?php

test('registration screen can be rendered', function () {
    test()->markTestSkipped('Registration routes are not enabled in this application.');
});

test('new users can register and are assigned admin role', function () {
    test()->markTestSkipped('Registration routes are not enabled in this application.');
});